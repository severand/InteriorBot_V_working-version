# 📋 ДОПОЛНИТЕЛЬНОЕ ТЕХНИЧЕСКОЕ ЗАДАНИЕ
## Новые функции для InteriorBot

**Дата:** 17 Декабря 2025  
**Проект:** InteriorBot  
**Статус:** Draft  

---

## 1️⃣ СЕРВИС «ПРИМЕРКА ДИЗАЙНА»

**Цель:** Дать пользователю возможность «примерить» понравившийся дизайн (референс) на конкретное помещение/фото клиента.

### 1.1. Пользовательский сценарий
1. Пользователь загружает фото **своего помещения** (базовое фото).  
2. Пользователь выбирает уже **сгенерированный дизайн** из истории или из галереи референсов.  
3. Нажимает кнопку **«Примерить дизайн»**.  
4. Система отправляет оба изображения (базовое фото + референс-дизайн) в модель (Replicate или аналог), которая:
   - сохраняет геометрию комнаты (стены, пол, перспективу),
   - переносит композицию, цветовую палитру и стиль мебели из референса.
5. Пользователь получает результат:
   - Фото «до» (оригинал),
   - Фото «после» (новый дизайн на его помещении),
   - Кнопки «Скачать», «Опубликовать», «Сделать ещё вариант».

### 1.2. UI/UX
- В карточке каждого дизайна (в истории) добавить кнопку **«Примерить на мою комнату»**.
- В меню работы с текущим проектом — кнопку **«Примерить дизайн из галереи»**.
- После генерации результата — блок сравнения:
  - Инлайн-кнопки: «Показать ДО», «Показать ПОСЛЕ», «Показать 2 колонки» (две фотки рядом, как два сообщения или коллаж).

### 1.3. Технические требования
- Модель: использовать существующий стек (Replicate) или дополнительную модель типа *image-to-image с контролем композиции*.
- Передавать в модель:
  - фото помещения,
  - фото-референс,
  - параметры (уровень изменения, сохранение структуры, стиль).
- В БД:
  - таблица/расширение `designs`:
    - `base_photo_id`
    - `reference_design_id`
    - `try_on_result_photo_id`
    - `model_params`
    - `created_at`

---

## 2️⃣ КНОПКА «ОЧИСТИТЬ ЭКРАН» (EMPTY ROOM)

**Цель:** Получить «чистую» комнату — только стены, пол и потолок, без мебели и декора.

### 2.1. Пользовательский сценарий
1. Пользователь загружает фото комнаты или использует уже существующий дизайн.  
2. Нажимает кнопку **«Очистить экран»**.  
3. Система отправляет фото в модель удаления объектов (furniture removal / room emptying).  
4. Возвращает картинку **«пустой комнаты»**, которая становится базой для дальнейших переработок.

### 2.2. UI/UX
- В экране работы с фото добавить кнопку **«🧹 Очистить комнату»**.
- После обработки:
  - показать «до/после»,
  - предложить: «Сохранить как базу», «Сразу сгенерировать новый стиль поверх».

### 2.3. Технические требования
- Использовать модель, специализирующуюся на **object removal / clean room**.
- Гарантировать сохранение перспективы, основных контуров помещения, окон, дверей.
- Ограничения:
  - размеры/формат фото (например, до 4096x4096, JPG/PNG),
  - время обработки ≤ 30–40 сек.
- В БД:
  - `clean_base_photo_id`
  - ссылка на исходное фото
  - флаг `is_clean_base = true`.

---

## 3️⃣ КНОПКИ «СФОРМИРОВАТЬ СТИЛЬ» (1–2 ФОТО)

**Цель:** Научить бота формировать **единый стиль** на основе 1–2 загруженных фото.

### 3.1. Пользовательский сценарий
1. Пользователь загружает **1 или 2 фото** интерьеров, которые ему нравятся.  
2. Нажимает кнопку **«Сформировать стиль»**.  
3. Система:
   - анализирует изображения (цвета, материалы, формы, освещение),
   - формирует **описание стиля** (например: «Современный минимализм с акцентом на дерево и тёплый свет»),
   - сохраняет стиль в профиле пользователя.
4. Далее пользователь может:
   - применить этот стиль к своим комнатам,
   - использовать его во всех будущих генерациях.

### 3.2. Варианты работы
- **Объединить фото в общий стиль** — искать общие черты обеих картинок и формировать единый стиль.  
- **Создать два отдельных стиля** — по каждой фотке отдельно (опционально в будущем).

### 3.3. Технические требования
- Нужен сервис анализа изображений (vision-модель), который возвращает:
  - палитру (основные цвета),
  - материалы (дерево, металл, текстиль и т.п.),
  - тип мебели (лофт, классика, скандинавский и т.д.),
  - уровень заполненности (минимализм/максимализм).
- В БД (таблица `user_styles`):
  - `style_id`
  - `user_id`
  - `name`
  - `description`
  - `source_photos_ids` (1–2 id)
  - `style_params` (json — цвета, материалы, теги).

---

## 4️⃣ БИЗНЕС-СТИЛИ (ОТДЕЛЬНЫЙ РАЗДЕЛ)

**Цель:** Выделить отдельную линейку **бизнес-стилей** для коммерческих задач (офисы, склады, кафе, рестораны и т.п.).

### 4.1. Пользовательский сценарий
1. В главном/создающем меню пользователь видит отдельную кнопку **«Бизнес-стили»**.  
2. При нажатии открываются категории:
   - 🏢 Офис
   - 📦 Склад
   - ☕ Кафе
   - 🍽 Ресторан
   - 🏬 Шоурум / магазин
   - и др. (можно расширять через Google Sheets).
3. После выбора категории — показ **готовых пресетов** стилей (3–5+ на категорию).  
4. Пользователь выбирает стиль → загружает фото своего помещения → получает дизайн в выбранном бизнес-стиле.

### 4.2. Технические требования
- Добавить тип стиля `business` в системе стилей.  
- Для каждой категории:
  - подготовить набор промтов/параметров для модели,
  - хранить описание, примерные референсные изображения.
- В БД:
  - `business_styles` или поле `is_business = true` у стилей,
  - связь: `category` (office/warehouse/cafe/restaurant/etc).

### 4.3. UI/UX
- В «Сформировать стиль» показать секцию **«Готовые бизнес-стили»**.  
- Для каждого стиля — карточка: название, короткое описание, иконка или превью.

---

## 5️⃣ ЗАГРУЗКА 4 ФОТО ДЛЯ ФОРМИРОВАНИЯ СТИЛЯ

**Цель:** Расширить «Сформировать стиль» — дать возможность формировать **сложный, многогранный стиль** на основе до 4 фото.

### 5.1. Пользовательский сценарий
1. Пользователь загружает **до 4 фото** (например: гостиная, кухня, спальня, офис).  
2. Нажимает **«Сформировать стиль из 4 фото»**.  
3. Система анализирует все изображения:
   - ищет общие паттерны (цвет, материалы, мебель),
   - фиксирует различия, комбинирует их.
4. Результат:
   - единое стилевое описание,
   - набор тегов стиля,
   - готовый пресет для генерации.

### 5.2. Технические требования
- Ограничения:
  - до 4 фото за один стиль,
  - общий объём ≤ N МБ.
- Vision-модель анализирует каждый кадр, затем происходит **агрегация результатов**:
  - пересечение/объединение цветовых палитр,
  - пересечение используемых материалов,
  - доминирующие элементы.
- В БД:
  - поле `source_photos_ids` — массив из 1–4 id,
  - флаг `complex_style = true`.

---

## 6️⃣ НЕОГРАНИЧЕННОЕ ДОПОЛНЕНИЕ СТИЛЕЙ ЧЕРЕЗ GOOGLE SHEETS

**Цель:** Позволить **без разработчика** добавлять новые стили через Google Sheets — стили автоматически превращаются в кнопки в боте.

### 6.1. Концепция
- Есть один или несколько Google Sheets-документов, где менеджер/дизайнер:
  - добавляет новые стили,
  - редактирует названия,
  - меняет активность/приоритет.
- Бот регулярно синхронизируется и обновляет кнопки меню.

### 6.2. Структура Google Sheets
Минимальные поля:
- `style_id` — уникальный идентификатор стиля.
- `name` — название стиля.
- `description` — описание стиля.
- `type` — тип (residential / business / custom / promo).
- `category` — подкатегория (гостиная, офис, кафе и т.д.).
- `prompt` — текст промта для модели.
- `negative_prompt` — список нежелательных элементов.
- `is_active` — флаг, показывать ли стиль в боте.

### 6.3. Работа бота
1. При старте бота → запрос к Google Sheets API → загрузка всех стилей.  
2. Кеширование в памяти/БД на 24 часа.  
3. Возможность ручной команды админа `/sync_styles` → принудительная пересинхронизация.  
4. На основе списка активных стилей формируются inline-кнопки в меню выбора стиля.

### 6.4. Технические детали
- Интеграция с Google Sheets API (service account или OAuth).
- Обработка ошибок:
  - если Sheets недоступен → использовать последний кеш,
  - логирование ошибок синхронизации.
- В БД:
  - таблица `external_styles_cache` для сохранения последней версии.

---

## 7️⃣ ЛОГО ПРИ ФОРМИРОВАНИИ НОВЫХ ФОТО

**Цель:** Все сгенерированные дизайны помечать брендовым логотипом (водяной знак) — защита и маркетинг.

### 7.1. Пользовательский сценарий
- Для PRO/бизнес-аккаунтов — возможность **загрузить свой логотип**, который будет ставиться на все изображения.  
- Для обычных пользователей — логотип самого бота (например, InteriorBot).

### 7.2. Функционал
- Настройки логотипа:
  - позиция (нижний правый, нижний левый, верхний правый и т.д.),
  - размер (малый/средний/крупный, % от ширины),
  - прозрачность (0–100%).
- Логотип накладывается **после** завершения генерации и перед отправкой пользователю.

### 7.3. Технические требования
- Формат логотипа: PNG с прозрачностью.  
- Хранение:
  - либо в файловом хранилище (S3/аналог),
  - либо как file_id Telegram (если загружается через TG).
- В БД:
  - `user_logo_url` / `user_logo_file_id`,
  - настройки — `logo_position`, `logo_opacity`, `logo_scale`.

---

## 8️⃣ ПЕРЕКЛЮЧЕНИЕ «PRO-РЕЖИМ» / «СТАНДАРТНЫЙ РЕЖИМ»

**Цель:** Разделить функционал и лимиты между бесплатными и платными пользователями.

### 8.1. Режимы
**Стандартный режим:**
- Лимит генераций в сутки (например, 3–5).
- Ограниченный набор стилей.
- Обычная скорость генерации.

**PRO-режим:**
- Повышенные/безлимитные генерации.
- Доступ ко всем бизнес-стилям.
- Доступ к custom-стилям, логотипам, расширенным настройкам.
- Приоритет по очереди в очереди задач.
- Экспорт в высоком разрешении.

### 8.2. UI/UX
- В личном кабинете:
  - отображение текущего режима (Standard/PRO),
  - кнопка **«Перейти в PRO»**,
  - информация о преимуществах PRO.
- В главном меню — индикатор статуса.

### 8.3. Технические требования
- В БД поле `plan` или `is_pro` для пользователя.  
- Логика проверки лимитов:
  - таблица `usage_limits` (user_id, date, count).
- Интеграция с платежами (YooKassa/подписка).  
- Отдельная таблица `subscriptions` (user_id, start_at, end_at, plan).

---

## 9️⃣ ДОПОЛНИТЕЛЬНЫЕ СТИЛИ (CUSTOM USER STYLES)

**Цель:** Позволить пользователю создавать свои собственные стили и повторно использовать их.

### 9.1. Пользовательский сценарий
1. Пользователь заходит в **«Мои стили»** в личном кабинете.  
2. Нажимает **«Создать стиль»**.  
3. Заполняет:
   - название стиля,
   - краткое описание,
   - загружает 1–4 референсных изображения,
   - (опционально) задаёт ключевые слова (например, «лофт, бетон, тёмное дерево»).
4. Система генерирует из этого **структуру стиля** (prompt/параметры).  
5. При генерации дизайнов пользователь может выбрать этот стиль из списка «Мои стили».

### 9.2. Ограничения и правила
- Ограничить количество custom-стилей на пользователя (например, 10–20).  
- Возможность редактировать/удалять свои стили.  
- В будущем — возможность сделать стиль публичным (для маркетплейса стилей).

### 9.3. Технические требования
- Таблица `user_custom_styles`:
  - `style_id`
  - `user_id`
  - `name`
  - `description`
  - `photos_ids`
  - `prompt_params`
  - `created_at`, `updated_at`

---

## 🔟 ПУБЛИКАЦИЯ В TELEGRAM-КАНАЛЫ

**Цель:** Один клик — и дизайн публикуется в выбранный канал владельца.

### 10.1. Пользовательский сценарий
1. Пользователь подключает свои каналы (где он админ) через **/connect_channel**.  
2. После генерации дизайна появляется кнопка **«Опубликовать в канал»**.  
3. При нажатии:
   - бот предлагает выбрать канал из списка подключённых,
   - предлагает использовать ранее сгенерированное описание или создать новое.
4. Дизайн + описание публикуются в канал.

### 10.2. Технические требования
- Использовать `sendPhoto` / `sendMediaGroup` Telegram API.  
- Хранить привязку каналов к пользователю:
  - `user_channels` (user_id, channel_id, channel_title, permissions).
- При публикации — логировать:
  - `design_id`, `channel_id`, `message_id`, `posted_at`.

---

## 1️⃣1️⃣ ОПИСАНИЕ К ФОТОГРАФИЯМ (MANUAL)

**Цель:** Дать пользователю возможность вручную дописать/скорректировать текст к фото (описание, статья, пост).

### 11.1. Функционал
- После генерации дизайна:
  - показать поле ввода текста (до 500 символов),
  - подсказка по структуре (1–2 предложения, хэштеги, ссылка на бота),
  - возможность сохранить описание вместе с дизайном.
- При публикации в канал/соцсети — использовать сохранённый текст.

### 11.2. Технические требования
- Таблица `designs` дополнить полями:
  - `manual_description`,
  - `manual_hashtags`.

---

## 1️⃣2️⃣ ИНТЕГРАЦИЯ С PINTEREST

**Цель:** Автоматизировать публикацию дизайнов как «пинов» в Pinterest для продвижения и трафика.

### 12.1. Сценарий
- Подключение аккаунта Pinterest.
- Выбор доски.
- Формирование пина из:
  - изображения дизайна,
  - заголовка,
  - описания,
  - ссылки на бота/лендинг.

---

## 1️⃣3️⃣ ИНТЕГРАЦИЯ С ВКОНТАКТЕ

**Цель:** Публикация дизайнов в VK (личная страница, группы, паблики).

### 13.1. Сценарий
- Авторизация через VK OAuth.
- Выбор группы/страницы.
- Формирование поста: фото + текст + хэштеги.

---

## 1️⃣4️⃣ ИНТЕГРАЦИЯ С INSTAGRAM

**Цель:** Публикация дизайнов в Instagram (Feed, Stories, Reels) для визуального продвижения.

### 14.1. Сценарий
- Подключение бизнес-аккаунта IG.
- Выбор формата (пост/сторис).
- Формирование медиа и подписи.

---

## 1️⃣5️⃣ AI-ГЕНЕРАЦИЯ ОПИСАНИЙ И ПОСТОВ

(Уже подробно описано — оставить как есть, использовать как «мастер»-ТЗ для текстов.)

---

## 1️⃣6️⃣ КАТАЛОГ МЕБЕЛИ ДЛЯ ВСТРАИВАНИЯ В ДИЗАЙН

**Цель:** Дать возможность использовать **конкретные объекты мебели из каталога** (диваны, столы, стеллажи и т.д.) внутри генерируемого дизайна с **минимальным расходом токенов и ресурсов**.

### 16.1. Концепция
- В системе появляется отдельный **каталог мебели**:
  - каждая единица мебели имеет свою карточку (название, тип, бренд, фото/3D-рендер, габариты, цвет, материал),
  - мебель может быть как «общая» (базовая библиотека), так и «брендовая» (от производителей).
- При генерации дизайна пользователь (или система) может:
  - указать, какую мебель **обязательно использовать**,
  - указать допустимую замену (пример: «любой диван 3-местный, светлый, без ножек»).

### 16.2. Пользовательский сценарий (для пользователя/дизайнера)
1. Пользователь загружает фото комнаты или очищает её кнопкой «Очистить экран».  
2. Переходит в раздел **«Каталог мебели»** или видит блок «Добавить мебель из каталога».
3. Выбирает объекты мебели:
   - из категорий (диваны, столы, шкафы, стеллажи, кресла и т.д.),
   - по фильтрам: стиль, цвет, размер, бренд.
4. Отмечает галочками, какие элементы нужно использовать в дизайне.
5. Запускает генерацию — дизайн строится **вокруг выбранной мебели**.

### 16.3. Минимизация расхода токенов
- Вместо передачи в model полный текстовый список мебели каждый раз → **использовать ID-шники и готовые промт-шаблоны**:
  - в БД хранится словарь: `furniture_id → prompt_fragment` (например: «modern beige 3-seater sofa with wooden legs»),
  - в промт передаётся **только список ID**, а на стороне бэкенда мапится в текст **до запроса к модели** (локально, без LLM).
- Шаблоны промтов заранее подготовлены → не нужно каждый раз вызывать GPT для описания мебели.  
- Для сложных комбинаций мебели — использовать заранее заготовленные паттерны («диван + журнальный столик + торшер в углу»).

### 16.4. Структура каталога
- Таблица `furniture_catalog`:
  - `id`
  - `name`
  - `brand`
  - `type` (sofa, table, chair...)
  - `style` (loft, modern, classic...)
  - `color`
  - `material`
  - `dimensions`
  - `photo_url` или `render_url`
  - `prompt_fragment`
  - `is_active`
- Таблица `design_furniture_links`:
  - `design_id`
  - `furniture_id`
  - `position_info` (опционально: зона/угол комнаты).

### 16.5. UI/UX
- Отдельный блок **«Мебель»** в процессе генерации:
  - «Добавить из каталога» → открывается список мебели с фильтрами,
  - «Очистить выбор мебели».
- После генерации в карточке дизайна — блок:
  - «Использованная мебель» с ссылками на позиции из каталога.

---

## 1️⃣7️⃣ СЕРВИС «ПРИМЕРКА МЕБЕЛИ ДЛЯ ПРОИЗВОДИТЕЛЕЙ»

**Цель:** Дать **производителям мебели и оборудования** возможность «примерить» свои модели (каталоги) в реальных интерьерах — как в готовых дизайнах, так и на фото помещений.

### 17.1. Целевая аудитория
- Производители мебели.
- Дистрибьюторы, салоны мебели.
- B2B-партнёры, которым нужно показать, как их продукт смотрится в разных интерьерах.

### 17.2. Пользовательский сценарий (производитель)
1. Производитель заходит в **кабинет производителя** (отдельный тип аккаунта / роль).  
2. Загружает **каталог своей мебели**:
   - фото/рендеры мебели (изолированные на нейтральном фоне или PNG без фона),
   - название, модель, габариты, цвет, материал.
3. Выбирает режим:
   - **«Примерить в готовый дизайн»** (использовать уже сгенерированные интерьеры из базы InteriorBot),
   - **«Примерить в комнату клиента»** (загрузить фото помещения).
4. Отмечает, какой объект мебели нужно разместить:
   - пример: «этот диван вместо текущего в комнате»,
   - или: «добавить этот стеллаж вдоль правой стены».
5. Система:
   - анализирует текущую сцену (перспектива, освещение, стиль),
   - «встраивает» мебель производителя в сцену:
     - правильно масштабирует,
     - подстраивает освещение/тени,
     - по возможности заменяет похожий предмет.
6. Производитель получает результат:
   - изображение с его мебелью в комнате,
   - возможность скачать, отправить клиенту, разместить в каталоге.

### 17.3. Технические требования
- Отдельная таблица `manufacturer_accounts` и `manufacturer_furniture`:
  - `manufacturer_id`
  - `furniture_id`
  - все характеристики мебели.
- Модели:
  - использовать подход **object insertion / replace**:
    - либо через специализированные модели (inpainting + controlnet),
    - либо через комбинацию «очистить зону» + «вставить объект по описанию».
- Важно: минимально использовать LLM там, где можно обойтись структурированными данными:
  - описание мебели задаётся один раз при загрузке,
  - повторно только подставляется в промт.

### 17.4. UI/UX
- Для производителя отдельный вход/ролевая модель:
  - в боте кнопка «Кабинет производителя»,
  - краткий onboarding, как загружать каталог.
- В интерфейсе примерки:
  - выбор комнаты/дизайна,
  - выбор мебели,
  - выбор сценария (заменить существующую мебель / добавить новую).

### 17.5. Возможные названия сервиса
- «Примерка мебели для брендов».
- «B2B-модуль для производителей».
- «Каталог-обкатка».

---

## 📊 ИТОГОВАЯ ТАБЛИЦА

| # | Функция | Тип | Приоритет | Примечание |
|---|---------|-----|-----------|------------|
| 1 | Примерка дизайна | Feature | HIGH | Реплика дизайна на фото клиента |
| 2 | Очистить экран | Feature | HIGH | База для новых дизайнов |
| 3 | Сформировать стиль (1–2 фото) | Feature | HIGH | Персональные стилевые пресеты |
| 4 | Бизнес-стили | Feature | HIGH | Монетизация B2B сегмента |
| 5 | Стиль из 4 фото | Feature | MEDIUM | Комплексный стиль |
| 6 | Google Sheets Sync стилей | System | HIGH | Быстрое добавление стилей без кода |
| 7 | Логотип на фото | Feature | LOW | Брендинг и защита |
| 8 | Pro/Standard режимы | Subscription | HIGH | Подписочная модель |
| 9 | Custom User Styles | Feature | MEDIUM | Персонализация |
| 10 | Публикация в каналы | Integration | MEDIUM | TG продвижение |
| 11 | Описание фотографий (manual) | Feature | LOW | Ручная правка текста |
| 12 | Pinterest интеграция | Integration | MEDIUM | Дизайн-аудитория |
| 13 | VK интеграция | Integration | MEDIUM | Российская аудитория |
| 14 | Instagram интеграция | Integration | HIGH | Визуальный маркетинг |
| 15 | AI-генерация описаний | Feature | HIGH | Автоматизация контента |
| 16 | Каталог мебели | Feature | HIGH | Встраивание конкретных моделей мебели |
| 17 | Примерка мебели для производителей | B2B Feature | HIGH | Модуль для брендов |

---

**Документ содержит 17 функций для расширения InteriorBot.**
